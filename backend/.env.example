PORT=3000
JWT_ACCESS_SECRET=change_me_access_secret
JWT_ACCESS_EXPIRES_IN=1d

# Option A (recommended for production): single connection string
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Option B: individual Postgres connection parameters
DB_HOST=localhost
DB_PORT=5438
DB_USER=admin
DB_PASSWORD=admin123
DB_NAME=feeltflow

# Enable SSL (often required in production managed Postgres)
# DB_SSL=true
# DB_SSL_REJECT_UNAUTHORIZED=false